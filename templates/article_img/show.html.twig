{% extends 'base.html.twig' %}

{% block title %}ArticleImg{% endblock %}

{% block body %}
<style>
.formulaire{
    margin-top:70px;
    box-shadow: 0 8px 16px 0 rgba(0,0,0,0.2), 0 6px 20px 0 rgba(0,0,0,0.19);
    width:40%;
    padding-right:55px;
    padding-left:55px;
    height:50%;  
}

</style>
{{include('includes/header.html.twig')}}
   <br>

<div class="container formulaire card">
    <h2 class="display-4">Voir toutes les informations </h2>


    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article_img.id }}</td>
            </tr>
            <tr>
                <th>Filename</th>
                <td>{{ article_img.filename }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ article_img.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ article_img.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ article_img.prix }}</td>
            </tr>
            <tr>
                <th>Date_de_creation</th>
                <td>{{ article_img.dateDeCreation ? article_img.dateDeCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ article_img.auteur }}</td>
            </tr>
            <tr>
                <th>Active</th>
                <td>{{ article_img.active ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
</div>

    <a href="{{ path('article_img_index') }}">back to list</a>

    <a href="{{ path('article_img_edit', {'id': article_img.id}) }}">edit</a>

    {{ include('article_img/_delete_form.html.twig') }}


{% endblock %}
