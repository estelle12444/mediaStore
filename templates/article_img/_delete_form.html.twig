
<form method="post" action="{{ path('article_img_delete', {'id': article_img.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_img.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
