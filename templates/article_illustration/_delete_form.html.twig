<form method="post" action="{{ path('article_illustration_delete', {'id': article_illustration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ article_illustration.id) }}">
    <button class="btn">Delete</button>
</form>
